namespace BankMore.Shared.Domain.Enums
{
    public enum TipoFalha
    {
        INVALID_DOCUMENT,
        USER_UNAUTHORIZED,
        INVALID_ACCOUNT,
        INACTIVE_ACCOUNT,
        INVALID_VALUE,
        INVALID_TYPE
    }
}